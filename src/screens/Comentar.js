import React, { Component } from "react";
import { View, Text } from "react-native";

class Comentar extends Component {
    constructor(props) {
        super(props);
        this.state = {
            
        };
    }

    
    render() {
        return (
            <View >
                <Text>Pantalla de Comentar</Text>
            </View>

        );
    }
}

export default Comentar;